export const Utils = {
    // copyPartOfBuffer: function (dataStart, dataStop, bufferFrom, bufferTo) {
    //     let channels = playerFrom.buffer.get().numberOfChannels;
    //     let sampleRate = playerFrom.context.sampleRate;
    //     let start = Math.max(0, Math.floor(bufferFrom.length * dataStart));
    //     let stop = Math.min(Math.floor(bufferFrom.length * dataStop), bufferFrom.length);
    //     // playerTo.buffer._buffer = playerTo.context.createBuffer(channels, stop - start, sampleRate);
    //     playerTo.buffer = Tone.context.createBuffer(channels, stop - start, sampleRate);
    //     //this.context.createBufferSource();
    //     for (let i = 0; i < channels; i++) {
    //         let rootBuffer = playerFrom.buffer.get().getChannelData(i);
    //         playerTo.buffer.copyToChannel(rootBuffer.slice(start, stop), i);
    //         //playerTo._buffer.loaded = true;
    //         //playerTo.buffer._buffer.copyToChannel(rootBuffer.slice(start, stop), i);
    //         //playerTo._buffer.loaded = true;
    //     }
    // }
}